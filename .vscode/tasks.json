{
  "tasks": [
    {
      "type": "shell",
      "label": "g++ build library",
      "command": "/usr/bin/g++",
      "args": [
        "-Og",
        "-g",
        "-Ilibcapgen",
        "${workspaceFolder}/libcapgen/**/*.cpp",
        "-o",
        "${workspaceFolder}/bin/libcapgen"
      ]
    }
  ],
  "version": "2.0.0"
}
